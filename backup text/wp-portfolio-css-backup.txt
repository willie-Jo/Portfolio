* {
	box-sizing: border-box;
}
:root {
	--primary: #0d6efd;
	--primary-dark: #0b5ed7;
	--light: #f8f9fa;
	--text: #333;
	--border: #e5e7eb;
}
html {
	scroll-behavior: smooth;
}
body {
	transition: 0.3s;
	font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
	background: #f1f5f9;
	padding-left: auto;
	padding-right: auto;
}
small { 
	color: #cbd5e0;
}
h1, h2, h3 {
	font-family: Georgia, 'Times New Roman', serif;
	margin-bottom: 25px;
	letter-spacing: -0.5px;
}
a {
	cursor: pointer;
	transition: all 0.2s ease;
}
section {
	background: #ffffff;
	padding: 60px 20px;
	border: 1px solid #e5e7eb;
	border-radius: 10px;
	margin: 25px auto;
	font-family: system-ui, -apple-system, sans-serif;
	box-shadow: 0 12px 30px rgba(15, 23, 42, 0.15);
	max-width: 1100px;
}
.cards {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    background: #f8fafc;
    border: 1px solid #e5e7eb;
    border-radius: 10px;
    padding: 22px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    margin: 0; /* Clears any default margin issues blocking the gap */
    box-sizing: border-box;
}
.cards:hover {
	transform: translateY(-4px);
	transition: all 0.3s ease;
	box-shadow: 0 14px 28px rgba(0,0,0,0.10);
}
/*  Header, Navigation & Footer  */
.flex {
	display: flex;
	justify-content: space-between;
	align-items: flex-end;
	padding: 10px 20px;
	border-bottom: 2px solid #ccc;
	background-color: #1a202c;
	color: white; 
}
.flex div h1 {
	color: grey;
	font-weight: 700;
	font-size: 45px;
	margin-bottom: 0;
}
.flex div small {
	padding-left: 20px;
}
.flex nav {
	display: flex;
	gap: 15px;
}
nav a {
	display: inline-block;
	text-decoration: none;
	font-size: 1.2em;
	color: #cbd5e0;
	font-weight: 500;
}
nav a:hover {
	transform: translateY(-3px);
}
.btn.whatsapp {
	position: fixed;
	bottom: 70px;
	right: 5px;
	background: #25D366;
	color: white;
	padding: 14px 10px;
	border-radius: 50px;
	text-decoration: none;
	font-weight: 600;
	display: inline-flex;
	align-items: center;
}
.btn.whatsapp:hover { background: #1a202c;}
footer {
	text-align: center;
	padding: 10px;
	line-height: 1.2;
	font-size: 0.95rem;
	border-top: 3px solid #e5e7eb;
}
footer small {
	color: #555;
}

/* Project button style for home page and project page */
.tech{
	margin-bottom: 10px;
	font-size: 13px;
	color: #666;
	background: #f4f4f4;
	padding: 4px 10px;
	border-radius: 6px;
	width: fit-content;
}

/* project links */
.project-links {
	display: inline-grid;
	grid-template-columns: 1fr 1fr;
	gap: 10px; 
}

/* Project button */
.project-btn {
	padding: 10px 16px;
	border-radius: 8px;
	text-decoration: none;
	font-weight: 600;
	font-size: 14px;
	width: fit-content;
	transition: background 0.3s ease, transform 0.2s ease;
}

/* view-btn */
.view-btn {
	display: inline-block;
	background: #0073aa;
	color: white;
}
.view-btn:hover {
	background: #005f8d;
	transform: scale(1.05);
}
/* code-btn */
.code-btn {
	background: #f3f4f6;
	color: #111;
	border: 1px solid #d1d5db;
	/*width: fit-content;*/
}
.code-btn:hover { 
	background: #e5e7eb;
	transform: scale(1.05); 
}

/* ====== HOME PAGE STYLE ===== */

/* Profile styled Hero cover */
.hero-profile-wrap {
	position: relative;
	max-width: 1100px;
	margin: 25px auto 25px;
	box-shadow: 0 12px 30px rgba(15, 23, 42, 0.15);
	border: 1px solid;
	border-radius: 11px;
	overflow: hidden;
}

/* Dark top cover band */
.hero-cover {
	height: 240px;
	background: linear-gradient(rgba(15, 23, 42, 0.65), rgba(15, 23, 42,0.65)), url("file_000000008ea871f5add570d7c9e903a4.png");
	background-size: cover;
	background-position: center center;
	background-repeat: no-repeat;
	border-top-left-radius: 10px;
	border-top-right-radius: 10px;
}

/* Content sits partly over the dark cover */
.hero-profile-content {
	position: relative;
	margin-top: -99px;
	padding: 0 25px 10px;
	text-align: center;
}

/* Profile image overlaps the cover */
.hero-profile-content img {
	width: 180px;
	height: 180px;
	border-radius: 50%;
	border: 6px solid #ffffff;
	box-shadow: 0 10px 24px rgba(0,0,0,0.18);
	margin: 0 auto 20px;
	display: block;
	background: #fff;
}

/* Name */
.hero-profile-content h1 {
	margin: 0 0 8px 0;
	font-size: 36px;
	line-height: 1.2;
}

/* Tagline */
.hero-profile-content h3 {
	margin: 0 0 14px 0;
	font-size: 20px;
	font-weight: 500;
	color: #555;
	line-height: 1.4;
}

/* Intro text */
.hero-profile-content p {
	max-width: 760px;
	margin: 0 auto;
	line-height: 1.8;
	font-size: 17px;
}

/* Home page services Grid */
.services-grid {
	display: grid;
	grid-template-columns: repeat(2, minmax(0, 1fr));
	gap: 22px;
	margin-top: 25px;
}

/* Project Grid */
.project-grid {
	display: grid;
	grid-template-columns: repeat(2, minmax(0, 1fr));
	gap: 22px;
	margin-top: 25px;
}

/* Home page project Card */
.project-card {
	position: relative;
}

/* see-more project link */
.project-card .see-more {
	position: absolute;
	bottom: 15px;
	right: 15px;
	color: #0073aa;
	text-decoration: none;
	font-weight: 600;
	transition: background 0.3s ease, transform 0.2s ease;
}
.project-card .see-more:hover {
	text-decoration: underline;
	transform: translateY(-4px);
}

/* Give both services and portfolio cards equal visual rythm */
.service-card, .project-card {
	min-height: 220px;
}

/* Back to top button */
#backToTop {
	position: fixed;           /* stick to screen, not page */
	bottom: 10px;              /* 10px from bottom */
	right: 30px;               /* 30px from right */
	background: #3498db;
	color: white;
	padding: 12px 16px;
	border: none;
	border-radius: 50%;       
	font-size: 18px;
	display: none;            /* Hidden by default */
	box-shadow: 0 4px 10px rgba(0,0,0,0.3);
	transition: 0.3s;
	z-index: 1000;            /* Stays on top of everything */
}
#backToTop:hover {
	background: #2980b9;
	transform: scale(1.1);
}

/* ===== SERVICES PAGE STYLE ====== */

.service-page-flex {
	display: flex;
	flex-direction: column;
	gap: 22px;
}

/* ===== PROJECT PAGE STYLE ====== */

/* The Grid Container row stack layout */
.project-page-grid {
    display: grid;
    grid-template-columns: 1fr; /* Forces exactly one card per row */
    row-gap: 22px; /* forces a 22px spacing between the rows */
}

/* ===== CONTACT PAGE STYLE ===== */

.contact-grid {
	display: grid;
	grid-template-columns: repeat(2, 1fr);
	gap: 22px;
}
.contact-info-card p {
	margin-bottom: 25px;
	line-height: 1.2;
}
.contact-link {
	color: #2563eb;
	font-weight: 600;
	text-decoration: none;
}
.contact-link:hover {
	text-decoration: underline;
}